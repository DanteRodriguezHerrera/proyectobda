<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" 
rel="stylesheet" 
integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" 
crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reseñas</title>
</head>
<body>
   <div class ="container">
     <h1 class = "mb-4">Entradas</h1>
     <a href="/entradas/new" class = "btn btn-success">Nueva entrada</a>

    <% entradas.forEach(entrada => { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title"><%= entrada.title %></h4>
                <div class="card-subtitle text-muted mb-2">
                    <%= entrada.creado.toLocaleDateString() %>
            </div>
            <div class = "card-text mb-2"><%= entrada.descripcion %></div>
            <a href="entradas/<%= entrada.slug %>" class="btn btn-primary">Ver mas</a>
            <a href="entradas/editar/<%= entrada.id %>" class="btn btn-info">Editar</a>
            <form action="/entradas/<%= entrada.id %>?_method=DELETE" method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger">Borrar</button>
            </form>
         </div>
        </div>
    <% }) %>

   </div>
</body>
</html>




